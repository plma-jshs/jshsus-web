services:
  mysql:
    ports:
      - '${MYSQL_HOST_PORT:-3307}:3306'

  redis:
    ports:
      - '${REDIS_HOST_PORT:-6379}:6379'

  api:
    environment:
      NODE_ENV: development
      DEV_AUTH_BYPASS: 'true'
      DEV_AUTH_STUDENT_NO: ${DEV_AUTH_STUDENT_NO:-9999}
