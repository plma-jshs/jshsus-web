# Nginx Proxy Manager already maintains trusted CDN ranges in ip_ranges.conf.
# Override only the header at the individual server block so direct-origin
# callers cannot spoof CF-Connecting-IP.
real_ip_header CF-Connecting-IP;
real_ip_recursive on;

client_max_body_size 12m;
