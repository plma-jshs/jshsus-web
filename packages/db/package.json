{
  "name": "@jshsus/db",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\"",
    "prebuild": "pnpm clean",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:bootstrap-core": "node scripts/bootstrap-core-data.cjs",
    "db:prepare-baseline": "node scripts/prepare-baseline-database.cjs",
    "db:deploy": "pnpm db:prepare-baseline && pnpm db:migrate && pnpm db:bootstrap-core",
    "db:backup": "node scripts/backup-database.cjs",
    "db:provision-cognito-students": "node scripts/provision-cognito-students.cjs",
    "db:assert-local": "node -e \"require('./scripts/local-seed-safety.cjs').assertLocalSeedAllowed(); console.log('Local database target verified.')\"",
    "db:bootstrap-local": "node scripts/bootstrap-local.cjs"
  },
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "3.1084.0",
    "drizzle-orm": "0.45.2",
    "mysql2": "3.22.6"
  },
  "devDependencies": {
    "drizzle-kit": "0.31.10",
    "typescript": "5.9.3"
  }
}
